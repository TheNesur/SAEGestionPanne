{% extends 'base.html.twig' %}

{% block title %}Erreur d'autorisation d'accès{% endblock %}

{% block body %}
<style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins';
        }

        body{
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color:#F1E1E1;
        }

        .wrapper{
            position: relative;
            width: 400px;
            height: 400px;
            background: transparent;
            border: 2px solid rgba(255, 255, 255, .5);
            border: 20px;
            backdrop-filter: blur(20px);
            box-shadow: 0 0 30px rgba(0, 0, 0, .5);
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }

        .wrapper .form-box{
            width: 100%;
            padding: 40px;
            text-align:center;
            background-color:#FC4141;
        }

        .btn{
            width: 100%;
            height: 45px;
            background: #162938;
            border: none;
            outline: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 1em;
            color: #fff;
            font-weight: 500;
        }

        .menu{
            font-size: .9em;
            color: #162938;
            text-align: center;
            font-weight: 500;
            margin: 25px 0 10px;
        }

        .menu a {
            color: #162938;
            text-decoration: none;
            font-weight: 600;
        }

        .menu a:hover{
            text-decoration: underline;
        }

        p{
            color: #162938;
            font-weight: 600;
            line-height:1.5;
        }

        h1{
            padding-bottom: 10%;
            text-align:center;
        }

        h2{
            padding-bottom: 3%;
            padding-top:3%;
            text-align:center;
        }

    </style>

<div class="wrapper">
    <div class="form-box">
        <h1>Vous ne pouvez pas accéder à cette page.</h1>
        <p>Il semble que l'accès à cette partie de l'application ne vous est pas permis.<br />
        Si cela n'est pas normal, veuillez contacter un administrateur.</p>
        <h2>Motif du refus d'accès :</h2>
        {% if app.request.attributes.get('exception').statusCode == '401' %}
            <p>Vous n'êtes pas connecté.</p>
            <div class="menu">
                <a href="{{ path('app_login') }}" class="register-link">Se connecter</a>
            </div>
        {% endif %}
        {% if app.request.attributes.get('exception').statusCode == '403' %}
            <p>Vous n'avez pas les autorisations nécessaires.</p>
            <div class="menu">
                <a href="{{ path('admin;index') }}" class="register-link">Retour à l'accueil</a>
            </div>
        {% endif %}
    </div>
</div>
{% endblock %}
